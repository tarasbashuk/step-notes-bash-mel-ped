extends layout

block contentNotes
  h1 #{title}
  form(method='POST', action='/notes/edit/'+note._id)
    .form-group
      label Title:
      input.form-control(name='title', type='text', value=note.title)
    .form-group
      label Body:
      textarea.form-control(name='body')= note.body
    br
    input.btn.btn-primary(type='submit', value='Submit')
